procedure nian {
}

procedure main {
}

procedure program {
}

// print
// print print;
// print read;
// print call;
// print while;
// print if;
// print then;
// print else;
// print procedure;
// print program;
// print Uses;
// print Follows;

// read
//read print;
// read read;
// read call;
// read while;
// read if;
// read then;
// read else;
// read procedure;
// read program;
// read Uses;
// read Follows;

// while
//while (print != 121) {}
// while (122 > read) {}
// while ((123 == purple) && (!((eunectes + lancet > 124) || (call <= green)))) {}
// while (125 + 126 / 130 < (127 - 128) * 129 ) {}
// while (((((print + read) % call) >= while * if) && (then - 131)) || ((!(else / 132 > 133)) && (!((procedure != program)) || (Uses + Follows < 134))) {}
// while (((((print + read) % call) >= while * if) && (then - Next)) || ((!(else / Affects > OR)) && (!((procedure != program)) || (Uses + Follows < AND))) {}

// if
// if (if + NOT < AND * (then)) then {} else {}
// if ((!(201 <= else)) || (Uses + procedure % program > Follows)) then {} else {}
// if (johnlee > zhongli) then {} else {}


// assign LHS
// johnlee = print;
// johnlee = 301;
// johnlee = read + 302;
// johnlee = 302 + read;
// cocogoat = call / while;
// cocogoat = (303 - call) / while * if;
// cocogoat = johnlee;
// xiao = johnlee + then;
// keqing = (program % (procedure + else - then) * if + (while - call + read) / print);
// keqing = cocogoat;
// ninguang = Follows * (Uses / program % (procedure + else - Uses / then) * if + (while - cocogoat % call + read) / print);
// johnlee = hutao;
// beidou = 304 + xiangling * program;
// xiangling = beidou + 305 % (if * then);

// assign RHS
// print = 401;
// read = 402 + xiangling - (johnlee + cocogoat) * keqing;
// call = childe;
// while = 404 * (johnlee / childe) + 403 % 405;
// if = childe + fatui / klee;
// then = (406 / 401);
// else = (((((407)))));
// procedure = print + read / call * (while - if * (then + else)) % procedure;
// program = program;
// Uses = print + print + print / (read - read - read) + (call + (call - call) * (while / while + while)) / (if * if * if - (then % then % then) % (else + else - else * else / else % else) + (procedure + procedure - procedure * (Uses - Uses % Uses)))
// Follows = program + childe + 407;



procedure procedure {
    print print;
    call print;

    while (a + b > c) {
	read print;
        call read;
        while (print != 121) {
            xiangling = beidou + 305 % (if * then);
            if (if + NOT < AND * (then)) then {
                johnlee = print;
                call while;
                johnlee = 301;
	    } else {
                johnlee = read + 302;
                call then;
	    }
            call if;
            print procedure;
	}
    }

}

procedure print {
    print read;
    while (122 > read) {
        johnlee = 302 + read;
        print = 401;
        print program;
    }
    call call;
    read read;
}

procedure read {
    read call;
    if (912 == 911+1) then {
        johnlee = 302 + read;
        print Uses;
        while ((123 == purple) && (!((eunectes + lancet > 124) || (call <= green)))) {
            call print;
            print call;
            beidou = 304 + xiangling * program;
        }
        read = 402 + xiangling - (johnlee + cocogoat) * keqing;
    } else {
        call while;
        cocogoat = call / while;
    }
}

procedure call {
    call while;
    while (((((print + read) % call) >= while * if) && (then - 131)) || ((!(else / 132 > 133)) && (!((procedure != program)) || (Uses + Follows < 134))) {

        print if;
        read if;
        cocogoat = johnlee;
        Follows = program + childe + 407;
        call = childe;
        read procedure;
    }
    print Follows;
}

procedure while {
    print then;
    read then;
    if ((!(201 <= else)) || (Uses + procedure % program > Follows)) then {
        johnlee = hutao;
        read program;
    } else {
        while (((((print + read) % call) >= while * if) && (then - Next)) || ((!(else / Affects > OR)) && (!((procedure != program)) || (Uses + Follows < AND))) {
            keqing = cocogoat;
            if (johnlee > zhongli) then {
                keqing = (program % (procedure + else - then) * if + (while - call + read) / print);
                if = childe + fatui / klee;
                Uses = print + print + print / (read - read - read) + (call + (call - call) * (while / while + while)) / (if * if * if - (then % then % then) % (else + else - else * else / else % else) + (procedure + procedure - procedure * (Uses - Uses % Uses)))
            } else {
                cocogoat = johnlee;
                xiao = johnlee + then;
            }
            while = 404 * (johnlee / childe) + 403 % 405;
        }
        ninguang = Follows * (Uses / program % (procedure + else - Uses / then) * if + (while - cocogoat % call + read) / print);
    }
    johnlee = hutao;

}

procedure if {
    call print;
    while (125 + 126 / 130 < (127 - 128) * 129 ) {
        if (912 != 911+a) then {
            call read;
            program = program;
	} else {
            read while;
            call while;
	}
        call then;
        cocogoat = (303 - call) / while * if;
        if (912+a != 911) then {
            call = childe;
            read Uses;
	} else {
            print while;
	}
    }
}

procedure then {
    print else;
    read else;
    else = (((((407)))));
    call while;
    procedure = print + read / call * (while - if * (then + else)) % procedure;
    then = (406 / 401);
    read Follows;
}

procedure else {
    johnlee = print;
    johnlee = 301;
    johnlee = read + 302;
    johnlee = 302 + read;
    cocogoat = call / while;
    cocogoat = (303 - call) / while * if;
    cocogoat = johnlee;
    xiao = johnlee + then;
    keqing = (program % (procedure + else - then) * if + (while - call + read) / print);
    keqing = cocogoat;
    ninguang = Follows * (Uses / program % (procedure + else - Uses / then) * if + (while - cocogoat % call + read) / print);
    johnlee = hutao;
    beidou = 304 + xiangling * program;
    xiangling = beidou + 305 % (if * then);


    print = 401;
    read = 402 + xiangling - (johnlee + cocogoat) * keqing;
    call = childe;
    while = 404 * (johnlee / childe) + 403 % 405;
    if = childe + fatui / klee;
    then = (406 / 401);
    else = (((((407)))));
    procedure = print + read / call * (while - if * (then + else)) % procedure;
    program = program;
    Uses = print + print + print / (read - read - read) + (call + (call - call) * (while / while + while)) / (if * if * if - (then % then % then) % (else + else - else * else / else % else) + (procedure + procedure - procedure * (Uses - Uses % Uses)))
    Follows = program + childe + 407;
}

procedure Uses {
}
